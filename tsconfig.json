// tsconfig.json
// typescript 관련 설정파일

///////////////////////////////////////////////////////
// include : typescript가 지켜볼 폴더

// compilerOption

// outDir : 컴파일 후 파일을 내보낼 폴더명

// target : 어떤 버전의 JS로 컴파일 할지 선택
// 대부분의 nodeJS와 브라우저가 es6를 지원함 (권장)

// lib : 타입스크립트에게 어떤 API를 사용하고 어떤 환경에서 코드를 실행하는지 지정할 수 있다. 런타임환경을 지정.
// 실행 환경에 따라서 사용하는 API에 대한 call signature를 지원해준다.
// ex) 브라우저에서 실행되면 "DOM"

// strict : 엄격한 타입검사 옵션을 활성화한다.

{
  "include": ["src"],
  "compilerOptions": {
    "outDir": "build",
    "target": "ES6",
    "lib": ["ES6", "DOM"],
    "strict": true,
    "allowJs": true
  }
}
